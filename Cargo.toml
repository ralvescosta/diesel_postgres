[package]
name = "diesel_postgre"
version = "0.1.0"
edition = "2018"

[dependencies]
diesel = { version = "1.4.4", features = ["postgres", "sqlite"] }
postgres = "0.19.1"
dotenv = "0.15.0"

[dev-dependencies]
mockall = "0.10.2"